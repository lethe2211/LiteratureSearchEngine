// Generated by CoffeeScript 1.8.0
(function() {
  var ready;

  ready = function() {
    var isExperimentalMode, json, url;
    isExperimentalMode = Cookie.getCookie('is_experimental_mode');
    if (isExperimentalMode === 'true') {
      alert('Ready to search');
      $('#search_results').show();
      $('#countdown_timer').countdown('resume');
      $.get('../../../logs/resume_countdown/' + gon.userid + '/' + gon["interface"], {}, json = function() {
        return console.log('../../../logs/resume_countdown' + gon.userid + '/' + gon["interface"]);
      });
    } else {
      $('.relevance').hide();
    }
    $('#status').text('Search completed');
    url = '../../../logs/page_loaded/' + gon.userid + '/' + gon["interface"];
    return $.get(url, {
      search_string: gon.query
    }, json = function() {
      return console.log(url);
    });
  };

  $(document).ready(ready);

  $(document).on('page:load', ready);

}).call(this);
